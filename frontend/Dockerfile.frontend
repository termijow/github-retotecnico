FROM node:18-alpine
WORKDIR /app
# Copia las dependencias
COPY package*.json ./
RUN npm ci
# Copia el código fuente
COPY . .
# Construye el proyecto
RUN npm run build
# Comando para servir el frontend
CMD ["npm", "start"]
EXPOSE 3000
